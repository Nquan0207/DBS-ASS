@model List<CreateSchedule>

<div class = "card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">CreateSchedule List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
                <h2 class="text-primary">CreateSchedule List</h2>
            </div>
            <div class="col-6 text-end">
                <a asp-controller="CreateSchedule" asp-action="UpsertStudent" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Create New CreateSchedule
                </a>
            </div>

        </div>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Student</th>
                    <th>Course</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var thing in Model)
                {
                    <tr>
                        <td>@thing.Student.Mssv</td>
                        <td>@thing.Schedule.ScheduleId</td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                @* <a asp-controller="CreateSchedule" asp-action="UpsertStudent" asp-route-id="@thing.LID" asp-route-id2="@thing.CourseID" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a> *@
                                <a asp-controller="CreateSchedule" asp-action="Delete" asp-route-id="@thing.Mssv" asp-route-id2="@thing.ScheduleID" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
